<manifest package="com.nicolasmilliard.socialcats.testsettings"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <application>

        <activity
            android:name="com.nicolasmilliard.socialcats.testsettings.TestSettingsActivity"
            android:label="@string/app_testsettings"
            android:icon="@mipmap/ic_testlauncher"
            android:launchMode="singleInstance"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>